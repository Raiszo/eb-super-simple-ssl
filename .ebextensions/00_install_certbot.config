packages:
    yum:
        epel-release: []
        mod24_ssl : []

# remember to use apache/nginx depending on you app
container_commands:
    00_install_certbot:
        test: test ! -d "/etc/letsencrypt/"
        ignoreErrors: true
        command: "sudo yum install -y certbot certbot-apache"
